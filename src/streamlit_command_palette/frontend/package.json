{
  "name": "streamlit-command-palette-frontend",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run clean && npm run typecheck && vite build",
    "clean": "rimraf build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@streamlit/component-v2-lib": "^0.2.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
